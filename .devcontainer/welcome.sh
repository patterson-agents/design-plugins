#!/usr/bin/env bash
# Printed every time a terminal attaches. Keep it short.
cat <<'EOF'

  Patterson Design — Claude Code Marketplace workbench
  ----------------------------------------------------
  claude    Claude Code   → /plugin marketplace add .   then /plugin install <name>@patterson-design
  copilot   Copilot CLI   → /login on first run (reads .github/copilot-instructions.md)
  opencode  OpenCode TUI  → /connect on first run
  gh        GitHub CLI    → gh auth status

  Tasks (Terminal → Run Task):  Validate plugins · Serve docs preview

EOF
