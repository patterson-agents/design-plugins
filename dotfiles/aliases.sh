# Patterson agent workbench aliases — sourced by ~/.bashrc / ~/.zshrc
alias cc="claude"
alias ccc="claude --continue"
alias oc="opencode"
alias cop="copilot"
alias plugval="claude plugin validate ."
alias pluglist="claude plugin list"
alias docs="npx --yes serve -l 3000 ."

# Register this repo as a local Claude Code marketplace (run once per machine)
alias mpadd="claude plugin marketplace add ."
