// Generated from src/content/tutorial — do not edit by hand; regenerate when lessons change.
window.TK_LESSONS = [
 {
  "id": "1-foundations/1-design-tokens/1-brand-tokens",
  "num": 1,
  "part": "Foundations",
  "chapter": "Design tokens",
  "title": "Define the Patterson brand tokens",
  "artifact": "brand-tokens.css",
  "html": "<h1>Define the Patterson brand tokens</h1>\n<p>A design system starts with <strong>tokens</strong> — named values that stand in for raw hex, pixel, and font values. Patterson's palette is carried by two brand colors, navy <code>#003767</code> and sky <code>#00A8E1</code>, with a cool gray for text. Product UI never hardcodes those hex values; it references a <em>semantic alias</em> (like <code>--accent</code>) that points at a primitive. That indirection is what lets the whole system re-theme from one place.</p>\n<div class=\"callout callout-info\"><div class=\"callout-title\">info</div><p><strong>What you'll build:</strong> a <code>brand-tokens.css</code> that declares the Patterson brand primitives and one semantic alias. <strong>How it's checked:</strong> the terminal runs <code>node validate.mjs</code>, which scrapes your <code>--custom-property</code> declarations and lints them against the brand.</p></div>\n<h2>Your task</h2>\n<p>Open <code>brand-tokens.css</code> in the editor. It defines <code>--pat-navy</code>, but the palette is incomplete and there's no semantic alias yet.</p>\n<p><strong>1.</strong> Add the two missing primitives — sky and gray — with their exact brand values. <strong>2.</strong> Add a semantic <code>--accent</code> alias that <strong>references</strong> the sky primitive with <code>var(…)</code> rather than repeating the hex.</p>\n<p>Your finished file should look like this:</p>\n<div class=\"code-block\"><div class=\"code-title\">brand-tokens.css</div><pre><code>:root {\n  /* Patterson brand primitives */\n  --pat-navy: #003767;\n  --pat-sky: #00A8E1;\n  --pat-gray: #58585B;\n\n  /* Semantic aliases — reference the primitives above */\n  --accent: var(--pat-sky);\n}</code></pre></div>\n<div class=\"callout callout-warn\"><div class=\"callout-title\">warn</div><p><code>--accent: #00A8E1;</code> would look identical on screen but the validator rejects it. Aliases must point at a primitive (<code>var(--pat-sky)</code>) so a single edit re-themes everything downstream — that's the whole point of a token layer.</p></div>\n<p>Run the validator in the terminal to check your work:</p>\n<div class=\"code-block\"><pre><code>node validate.mjs</code></pre></div>\n<div class=\"callout callout-success\"><div class=\"callout-title\">success</div><p>When every line reads <code>✓</code> and the terminal prints <strong>PASS — on brand!</strong>, you've got a valid Patterson token layer. This is exactly the pattern the full design system uses in <code>tokens/colors.css</code>.</p></div>\n<div class=\"callout callout-tip\"><div class=\"callout-title\">tip</div><p>Stuck? Press <strong>Solve</strong> to reveal the completed <code>_solution/</code> version, then compare it with yours. <strong>Reset</strong> puts your starting files back.</p></div>",
  "starter": ":root {\n  /* Patterson brand primitives */\n  --pat-navy: #003767;\n\n  /* Semantic aliases — reference the primitives above */\n}\n",
  "solution": ":root {\n  /* Patterson brand primitives */\n  --pat-navy: #003767;\n  --pat-sky: #00A8E1;\n  --pat-gray: #58585B;\n\n  /* Semantic aliases — reference the primitives above */\n  --accent: var(--pat-sky);\n}\n",
  "checksSrc": "  { label: 'Tokens are declared inside a :root { } block', test: (s) => /:root\\s*{/.test(s) },\n  { label: 'Defines --pat-navy with brand value #003767', test: (s) => /--pat-navy\\s*:\\s*#003767\\s*;/i.test(s) },\n  { label: 'Defines --pat-sky with brand value #00A8E1', test: (s) => /--pat-sky\\s*:\\s*#00A8E1\\s*;/i.test(s) },\n  { label: 'Defines --pat-gray with brand value #58585B', test: (s) => /--pat-gray\\s*:\\s*#58585B\\s*;/i.test(s) },\n  { label: '--accent references a primitive via var(--pat-…), not raw hex', test: (s) => { const m = s.match(/--accent\\s*:\\s*([^;]+);/i); return !!m && /var\\(\\s*--pat-/.test(m[1]); } },",
  "jsonMode": false
 },
 {
  "id": "2-agent-context/1-context-files/1-agents-md",
  "num": 2,
  "part": "Agent Context Files",
  "chapter": "Teaching agents your project",
  "title": "AGENTS.md — a README for agents",
  "artifact": "AGENTS.md",
  "html": "<h1>AGENTS.md — a README for agents</h1>\n<p><strong>AGENTS.md</strong> is an open convention: one plain-Markdown file at the repo root that tells <em>any</em> coding agent (Claude Code, Copilot, OpenCode, Cursor…) how to work in your project. There is no required schema — it wins by being predictable: agents look for it, read it, and follow it.</p>\n<p>A good AGENTS.md answers three questions fast:</p>\n<ol><li><strong>What is this project?</strong> One or two sentences of orientation.</li><li><strong>How do I run it?</strong> Exact setup, dev, and test commands in fenced blocks.</li><li><strong>What are the house rules?</strong> Code style, naming, what not to touch.</li></ol>\n<div class=\"callout callout-info\"><div class=\"callout-title\">info</div><p><strong>What you'll build:</strong> a complete <code>AGENTS.md</code> for the Patterson design-system repo. <strong>How it's checked:</strong> <code>node validate.mjs</code> lints the file for the sections agents rely on.</p></div>\n<h2>Your task</h2>\n<p>Open <code>AGENTS.md</code>. The overview is written; the two sections agents need most are missing.</p>\n<p><strong>1.</strong> Add a <code>## Setup commands</code> section with a fenced <code>sh</code> block containing the real commands (<code>npm install</code>, <code>npm run dev</code>).</p>\n<p><strong>2.</strong> Add a <code>## Code style</code> section with at least two concrete rules — e.g. <em>\"reference tokens, never raw hex\"</em> and <em>\"components live in <code>ui_kits/</code>\"</em>.</p>\n<div class=\"code-block\"><div class=\"code-title\">AGENTS.md</div><pre><code># Patterson Design System\n\nBrand tokens, templates, and UI kits for Patterson Companies.\nDeliverables are static HTML + React; no bundler.\n\n## Setup commands\n</code></pre></div>\n<p>(fenced sh block with npm install / npm run dev)</p>\n<div class=\"code-block\"><pre><code>\n## Code style\n\n- Reference tokens (\\`var(--pat-sky)\\`), never raw hex in components.</code></pre></div>\n<p>Run the validator:</p>\n<div class=\"code-block\"><pre><code>node validate.mjs</code></pre></div>\n<div class=\"callout callout-tip\"><div class=\"callout-title\">tip</div><p>Keep AGENTS.md <strong>imperative and specific</strong>. \"Run <code>npm run check</code> before committing\" beats a paragraph about testing philosophy. Agents follow commands, not vibes.</p></div>",
  "starter": "# Patterson Design System\n\nBrand tokens, templates, and UI kits for Patterson Companies.\nDeliverables are static HTML + React; no bundler.\n",
  "solution": "# Patterson Design System\n\nBrand tokens, templates, and UI kits for Patterson Companies.\nDeliverables are static HTML + React; no bundler.\n\n## Setup commands\n\n```sh\nnpm install\nnpm run dev     # serves the kit previews on :4321\n```\n\n## Code style\n\n- Reference tokens (`var(--pat-sky)`), never raw hex in components.\n- Components live in `ui_kits/`; templates in `templates/`.\n- Two-space indentation; semantic HTML first.\n",
  "checksSrc": "  { label: 'Top-level # heading orients the agent', test: (s) => /^# .+/m.test(s) },\n  { label: 'Has a \"## Setup commands\" section', test: (s) => /^## Setup commands/mi.test(s) },\n  { label: 'Setup commands are in a fenced sh block with npm install', test: (s) => /```(sh|bash)[\\s\\S]*?npm (install|i)\\b[\\s\\S]*?```/.test(s) },\n  { label: 'Has a \"## Code style\" section with at least two rules', test: (s) => { const m = s.match(/^## Code style([\\s\\S]*?)(^## |$(?![\\s\\S]))/mi); return !!m && (m[1].match(/^- /gm) || []).length >= 2; } },",
  "jsonMode": false
 },
 {
  "id": "2-agent-context/1-context-files/2-claude-md",
  "num": 3,
  "part": "Agent Context Files",
  "chapter": "Teaching agents your project",
  "title": "CLAUDE.md — memory, not manuals",
  "artifact": "CLAUDE.md",
  "html": "<h1>CLAUDE.md — memory, not manuals</h1>\n<p>Claude Code loads <strong>CLAUDE.md</strong> into context at the start of <em>every</em> session. That makes it the most expensive file in your repo: every line is a recurring token cost, whether or not it's relevant to the task at hand.</p>\n<p>The rule of thumb from Anthropic's own guidance: CLAUDE.md holds <strong>facts and conventions</strong>; <strong>procedures become skills</strong>. A skill's body loads only when invoked — long reference material costs almost nothing until you need it.</p>\n<div class=\"callout callout-warn\"><div class=\"callout-title\">warn</div><p>The starter file below has grown a 10-step deployment <em>procedure</em>. That's the classic smell: a section of CLAUDE.md that reads like a runbook belongs in <code>.claude/skills/deploy/SKILL.md</code>, invoked as <code>/deploy</code>.</p></div>\n<h2>Your task</h2>\n<p>Open <code>CLAUDE.md</code> and put it on a diet:</p>\n<p><strong>1.</strong> Delete the step-by-step \"Deployment procedure\" section entirely.</p>\n<p><strong>2.</strong> Replace it with one line of <em>memory</em>: deployment is handled by the <code>/deploy</code> skill.</p>\n<p><strong>3.</strong> Keep the file under 40 lines. Facts, commands, conventions — nothing else.</p>\n<div class=\"code-block\"><div class=\"code-title\">CLAUDE.md</div><pre><code>## Deployment\n\nDeployment procedure:\nStep 1. Run the test suite…\nStep 2. Build the bundle…\n…\nStep 10. Announce in #releases.\n\nDeployment: run \\`/deploy\\` (see .claude/skills/deploy/).</code></pre></div>\n<p>Then check your work:</p>\n<div class=\"code-block\"><pre><code>node validate.mjs</code></pre></div>\n<div class=\"callout callout-success\"><div class=\"callout-title\">success</div><p>CLAUDE.md and AGENTS.md coexist happily: AGENTS.md is the cross-agent contract; CLAUDE.md adds the Claude-specific memory layer on top.</p></div>",
  "starter": "# CLAUDE.md\n\n## Project facts\n\n- Design system for Patterson Companies (dental + veterinary).\n- Static HTML + React, no bundler; tokens live in tokens/colors.css.\n- Brand: navy #003767, sky #00A8E1.\n\n## Conventions\n\n- Reference tokens, never raw hex, in components.\n- New templates go in templates/<slug>/.\n\n## Deployment\n\nDeployment procedure:\nStep 1. Run the test suite with npm run check.\nStep 2. Build the static bundle with npm run build.\nStep 3. Verify the bundle size is under 2 MB.\nStep 4. Copy dist/ to the staging bucket.\nStep 5. Smoke-test staging on Chrome and Safari.\nStep 6. Ask #design-systems for sign-off.\nStep 7. Promote staging to production.\nStep 8. Invalidate the CDN cache.\nStep 9. Tag the release in git.\nStep 10. Announce in #releases.\n",
  "solution": "# CLAUDE.md\n\n## Project facts\n\n- Design system for Patterson Companies (dental + veterinary).\n- Static HTML + React, no bundler; tokens live in tokens/colors.css.\n- Brand: navy #003767, sky #00A8E1.\n\n## Conventions\n\n- Reference tokens, never raw hex, in components.\n- New templates go in templates/<slug>/.\n\n## Deployment\n\nDeployment: run `/deploy` (see .claude/skills/deploy/).\n",
  "checksSrc": "  { label: 'Keeps the project facts section', test: (s) => /^## Project facts/mi.test(s) },\n  { label: 'The step-by-step procedure is gone', test: (s) => !/Step \\d/i.test(s) },\n  { label: 'Points to the /deploy skill instead', test: (s) => /\\/deploy/.test(s) },\n  { label: 'Stays concise — under 40 lines', test: (s) => s.trimEnd().split('\\n').length <= 40 },",
  "jsonMode": false
 },
 {
  "id": "2-agent-context/1-context-files/3-design-md",
  "num": 4,
  "part": "Agent Context Files",
  "chapter": "Teaching agents your project",
  "title": "design.md — design context for agents",
  "artifact": "design.md",
  "html": "<h1>design.md — design context for agents</h1>\n<p>AGENTS.md tells agents how to <em>build</em>; <strong>design.md</strong> tells them how things should <em>look</em>. It's the file a design-aware agent reads before generating any UI: the palette, the type stack, the voice, and the non-negotiables — in a form an agent can apply mechanically.</p>\n<p>Unlike AGENTS.md, design.md earns its keep with <strong>exact values</strong>. \"Use brand colors\" is useless to an agent; <code>--pat-navy: #003767</code> is executable.</p>\n<div class=\"callout callout-info\"><div class=\"callout-title\">info</div><p><strong>What you'll build:</strong> a <code>design.md</code> for Patterson with a tokens table, a voice section, and one hard rule about aliases.</p></div>\n<h2>Your task</h2>\n<p><strong>1.</strong> Add a <code>## Tokens</code> section containing the three brand primitives with exact hex values — navy <code>#003767</code>, sky <code>#00A8E1</code>, gray <code>#58585B</code>.</p>\n<p><strong>2.</strong> Add the alias rule: accents come from <code>var(--pat-sky)</code>, never repeated hex.</p>\n<p><strong>3.</strong> Add a <code>## Voice</code> section with two adjectives and one \"never\" (e.g. <em>plain, confident; never exclamation marks</em>).</p>\n<div class=\"code-block\"><div class=\"code-title\">design.md</div><pre><code>## Tokens\n\n| Token | Value |\n| --- | --- |\n| --pat-navy | #003767 |\n…\n\nAccents reference \\`var(--pat-sky)\\` — never raw hex.\n\n## Voice\n\nPlain, confident. Never exclamation marks.</code></pre></div>\n<div class=\"code-block\"><pre><code>node validate.mjs</code></pre></div>\n<div class=\"callout callout-tip\"><div class=\"callout-title\">tip</div><p>The Patterson marketplace ships this exact knowledge as the <code>patterson-brand</code> plugin — its skill <em>is</em> a design.md that installs itself. You'll meet it in the last part of this tutorial.</p></div>",
  "starter": "# design.md — Patterson Companies\n\nDesign context for coding agents. Read before generating any UI.\n",
  "solution": "# design.md — Patterson Companies\n\nDesign context for coding agents. Read before generating any UI.\n\n## Tokens\n\n| Token | Value | Role |\n| --- | --- | --- |\n| --pat-navy | #003767 | Primary surfaces, headers |\n| --pat-sky | #00A8E1 | Accent, links, focus |\n| --pat-gray | #58585B | Body text |\n\nAccents reference `var(--pat-sky)` — never raw hex in components.\n\n## Voice\n\nPlain, confident. Sentence case everywhere. Never exclamation marks.\n",
  "checksSrc": "  { label: 'Has a \"## Tokens\" section', test: (s) => /^## Tokens/mi.test(s) },\n  { label: 'All three primitives with exact hex (#003767, #00A8E1, #58585B)', test: (s) => /#003767/i.test(s) && /#00A8E1/i.test(s) && /#58585B/i.test(s) },\n  { label: 'States the alias rule with var(--pat-sky)', test: (s) => /var\\(--pat-sky\\)/.test(s) },\n  { label: 'Has a \"## Voice\" section with a \"never\"', test: (s) => /^## Voice/mi.test(s) && /never/i.test(s) },",
  "jsonMode": false
 },
 {
  "id": "3-agent-skills/1-writing-skills/1-first-skill",
  "num": 5,
  "part": "Agent Skills",
  "chapter": "Writing skills",
  "title": "Your first SKILL.md",
  "artifact": "SKILL.md",
  "html": "<h1>Your first SKILL.md</h1>\n<p>A <strong>skill</strong> is a folder with a <code>SKILL.md</code> inside: YAML frontmatter that tells the agent <em>when</em> to load it, and a Markdown body with the instructions it follows. The format is the open <strong>Agent Skills</strong> standard (agentskills.io), used by Claude Code and a growing list of other agents; browse shared ones at <strong>skills.sh</strong>.</p>\n<p>The spec is strict about exactly two fields:</p>\n<ul><li><strong><code>name</code></strong> — max 64 chars; lowercase letters, numbers, and hyphens only; no leading/trailing/consecutive hyphens; must match the folder name.</li><li><strong><code>description</code></strong> — max 1024 chars, non-empty, and written in the <strong>third person</strong>. It must say <em>what</em> the skill does <strong>and when to use it</strong> — this is the only text the agent sees when deciding whether to load the skill.</li></ul>\n<div class=\"callout callout-warn\"><div class=\"callout-title\">warn</div><p>The starter frontmatter breaks both rules: <code>Patterson-Deck-Helper</code> has uppercase letters, and <em>\"Helps with decks.\"</em> gives the agent nothing to match against.</p></div>\n<h2>Your task</h2>\n<p>Fix the frontmatter in <code>SKILL.md</code>:</p>\n<p><strong>1.</strong> Rename to a valid spec name: <code>patterson-deck-builder</code>.</p>\n<p><strong>2.</strong> Rewrite the description in the third person, stating what it does <em>and</em> when to use it — include the phrase <strong>\"Use when\"</strong> and real trigger words.</p>\n<div class=\"code-block\"><div class=\"code-title\">SKILL.md</div><pre><code>---\nname: Patterson-Deck-Helper\ndescription: Helps with decks.\nname: patterson-deck-builder\ndescription: Builds on-brand Patterson slide decks from the 16:9 HTML template. Use when the user asks for a deck, presentation, or slides.\n---</code></pre></div>\n<div class=\"code-block\"><pre><code>node validate.mjs</code></pre></div>\n<div class=\"callout callout-success\"><div class=\"callout-title\">success</div><p>In Claude Code this file would live at <code>.claude/skills/patterson-deck-builder/SKILL.md</code> and be invokable as <code>/patterson-deck-builder</code> — or loaded automatically whenever a request matches the description.</p></div>",
  "starter": "---\nname: Patterson-Deck-Helper\ndescription: Helps with decks.\n---\n\n# Patterson deck builder\n\nBuild the deck from templates/deck/, keeping the wave watermark and navy cover.\nValidate with node validate.mjs before delivering.\n",
  "solution": "---\nname: patterson-deck-builder\ndescription: Builds on-brand Patterson slide decks from the 16:9 HTML template. Use when the user asks for a deck, presentation, or slides.\n---\n\n# Patterson deck builder\n\nBuild the deck from templates/deck/, keeping the wave watermark and navy cover.\nValidate with node validate.mjs before delivering.\n",
  "checksSrc": "  { label: 'Frontmatter block present (--- … ---)', test: (s) => /^---\\n[\\s\\S]+?\\n---/.test(s) },\n  { label: 'name is spec-valid (lowercase kebab, ≤64 chars)', test: (s) => { const m = s.match(/^name:\\s*(.+)$/m); if (!m) return false; const n = m[1].trim(); return n.length <= 64 && /^[a-z0-9]+(-[a-z0-9]+)*$/.test(n); } },\n  { label: 'description says what AND when (\"Use when …\")', test: (s) => { const m = s.match(/^description:\\s*(.+)$/m); return !!m && /use when/i.test(m[1]) && m[1].trim().length >= 40; } },\n  { label: 'description is third person, under 1024 chars', test: (s) => { const m = s.match(/^description:\\s*(.+)$/m); return !!m && m[1].length <= 1024 && !/\\bI can\\b|\\bI will\\b|you can use this/i.test(m[1]); } },",
  "jsonMode": false
 },
 {
  "id": "3-agent-skills/1-writing-skills/2-progressive-disclosure",
  "num": 6,
  "part": "Agent Skills",
  "chapter": "Writing skills",
  "title": "Progressive disclosure",
  "artifact": "SKILL.md",
  "html": "<h1>Progressive disclosure</h1>\n<p>Skills load in three stages: <strong>metadata</strong> (~100 tokens, always in context), the <strong>SKILL.md body</strong> (loaded when the skill activates), and <strong>bundled files</strong> (<code>references/</code>, <code>scripts/</code>, <code>assets/</code> — loaded only if the task needs them).</p>\n<p>The context window is a public good. Anthropic's authoring guidance is blunt: assume the model is already smart, keep the body <strong>under 500 lines</strong> (ideally far less), and move reference material into linked files that cost nothing until read.</p>\n<div class=\"callout callout-warn\"><div class=\"callout-title\">warn</div><p>The starter <code>SKILL.md</code> narrates the entire Patterson color theory, spacing rationale, and accessibility appendix inline — dozens of lines the agent pays for on <em>every</em> activation, to answer questions it usually isn't asking.</p></div>\n<h2>Your task</h2>\n<p><strong>1.</strong> Cut the body down to the essentials: the quick-start fence and the three rules. Get the whole file <strong>under 40 lines</strong>.</p>\n<p><strong>2.</strong> Move the detail out: link it as <code><a href=\"references/REFERENCE.md\" target=\"_blank\" rel=\"noopener\">references/REFERENCE.md</a></code> under an \"Additional resources\" heading. (The Solve version ships that file too.)</p>\n<div class=\"code-block\"><div class=\"code-title\">SKILL.md</div><pre><code>## Additional resources\n\n- Color rationale, spacing scale, and a11y notes:\n  [references/REFERENCE.md](references/REFERENCE.md)</code></pre></div>\n<div class=\"code-block\"><pre><code>node validate.mjs</code></pre></div>\n<div class=\"callout callout-tip\"><div class=\"callout-title\">tip</div><p>Keep references <strong>one level deep</strong> from SKILL.md. A chain of file → file → file gets partially read (<code>head -100</code>) and the agent misses the payload.</p></div>",
  "starter": "---\nname: patterson-design-context\ndescription: Applies Patterson brand tokens, spacing, and voice when generating UI. Use when building any Patterson page, component, or deck.\n---\n\n# Patterson design context\n\n## Quick start\n\n```css\n:root { --pat-navy: #003767; --pat-sky: #00A8E1; --pat-gray: #58585B; }\n```\n\nRules: reference tokens, never raw hex; 8px spacing grid; sentence case.\n\n### Background, part 1\n\nColor is one of the most important aspects of any design system. Colors carry\nmeaning, and meaning carries brand equity. The navy we use, #003767, was chosen\nafter extensive research into the dental and veterinary markets, where trust and\ncalm are paramount. Sky #00A8E1 provides energetic contrast while remaining\nprofessional. When you consider spacing, remember that an 8px base grid has been\nindustry standard for a decade because it divides evenly into common viewport\nsizes. Accessibility matters too: always check contrast ratios against WCAG AA,\nwhich requires 4.5:1 for body text and 3:1 for large text.\n\n### Background, part 2\n\nColor is one of the most important aspects of any design system. Colors carry\nmeaning, and meaning carries brand equity. The navy we use, #003767, was chosen\nafter extensive research into the dental and veterinary markets, where trust and\ncalm are paramount. Sky #00A8E1 provides energetic contrast while remaining\nprofessional. When you consider spacing, remember that an 8px base grid has been\nindustry standard for a decade because it divides evenly into common viewport\nsizes. Accessibility matters too: always check contrast ratios against WCAG AA,\nwhich requires 4.5:1 for body text and 3:1 for large text.\n\n### Background, part 3\n\nColor is one of the most important aspects of any design system. Colors carry\nmeaning, and meaning carries brand equity. The navy we use, #003767, was chosen\nafter extensive research into the dental and veterinary markets, where trust and\ncalm are paramount. Sky #00A8E1 provides energetic contrast while remaining\nprofessional. When you consider spacing, remember that an 8px base grid has been\nindustry standard for a decade because it divides evenly into common viewport\nsizes. Accessibility matters too: always check contrast ratios against WCAG AA,\nwhich requires 4.5:1 for body text and 3:1 for large text.\n",
  "solution": "---\nname: patterson-design-context\ndescription: Applies Patterson brand tokens, spacing, and voice when generating UI. Use when building any Patterson page, component, or deck.\n---\n\n# Patterson design context\n\n## Quick start\n\n```css\n:root { --pat-navy: #003767; --pat-sky: #00A8E1; --pat-gray: #58585B; }\n```\n\nRules: reference tokens, never raw hex; 8px spacing grid; sentence case.\n\n## Additional resources\n\n- Color rationale, spacing scale, and a11y notes:\n  [references/REFERENCE.md](references/REFERENCE.md)\n",
  "checksSrc": "  { label: 'Whole file under 40 lines', test: (s) => s.trimEnd().split('\\n').length <= 40 },\n  { label: 'Keeps the quick-start code fence', test: (s) => /```css[\\s\\S]*?--pat-navy[\\s\\S]*?```/.test(s) },\n  { label: 'Links detail one level deep: references/REFERENCE.md', test: (s) => /references\\/REFERENCE\\.md/.test(s) },\n  { label: 'Description keeps its trigger keywords', test: (s) => { const m = s.match(/^description:\\s*(.+)$/m); return !!m && /Patterson/i.test(m[1]) && /use when/i.test(m[1]); } },",
  "jsonMode": false
 },
 {
  "id": "4-extending-claude-code/1-mcp-servers/1-mcp-json",
  "num": 7,
  "part": "Extending Claude Code",
  "chapter": "MCP servers",
  "title": "Wire up an MCP server",
  "artifact": "mcp.json",
  "html": "<h1>Wire up an MCP server</h1>\n<p><strong>MCP (Model Context Protocol)</strong> servers give an agent live tools — query a database, search a docs index, call an internal API. Claude Code reads them from a <code>.mcp.json</code> at the project root (shown here as <code>mcp.json</code>): each entry names a server and says how to launch it.</p>\n<p>Two details trip everyone up:</p>\n<ul><li>Every server needs a <strong><code>command</code></strong> plus an <strong><code>args</code> array</strong> — one string per argument, not one long string.</li><li>Paths to files a <em>plugin</em> bundles must use <strong><code>${CLAUDE_PLUGIN_ROOT}</code></strong>, because installed plugins run from a cache directory, not your repo.</li></ul>\n<div class=\"callout callout-warn\"><div class=\"callout-title\">warn</div><p>The starter file has a trailing comma (invalid JSON), a missing <code>args</code> array, and a hardcoded path. Agents fail silently on all three.</p></div>\n<h2>Your task</h2>\n<p>Fix <code>mcp.json</code> so the <code>patterson-assets</code> server launches:</p>\n<div class=\"code-block\"><div class=\"code-title\">mcp.json</div><pre><code>{\n  \"mcpServers\": {\n    \"patterson-assets\": {\n      \"command\": \"node\",\n      \"args\": [\"${CLAUDE_PLUGIN_ROOT}/servers/assets.mjs\"],\n      \"env\": { \"ASSET_INDEX\": \"${CLAUDE_PLUGIN_ROOT}/data/index.json\" }\n    }\n  }\n}</code></pre></div>\n<div class=\"code-block\"><pre><code>node validate.mjs</code></pre></div>\n<div class=\"callout callout-tip\"><div class=\"callout-title\">tip</div><p>In a skill that calls MCP tools, always use the fully-qualified name — <code>patterson-assets:search_assets</code> — so the agent finds the right server.</p></div>",
  "starter": "{\n  \"mcpServers\": {\n    \"patterson-assets\": {\n      \"command\": \"node /Users/me/servers/assets.mjs\",\n      \"env\": { \"ASSET_INDEX\": \"/Users/me/data/index.json\" },\n    }\n  }\n}\n",
  "solution": "{\n  \"mcpServers\": {\n    \"patterson-assets\": {\n      \"command\": \"node\",\n      \"args\": [\"${CLAUDE_PLUGIN_ROOT}/servers/assets.mjs\"],\n      \"env\": { \"ASSET_INDEX\": \"${CLAUDE_PLUGIN_ROOT}/data/index.json\" }\n    }\n  }\n}\n",
  "checksSrc": "  { label: 'Valid JSON (no trailing commas)', test: (s, o) => !!o },\n  { label: 'Declares an mcpServers map', test: (s, o) => !!(o && o.mcpServers && typeof o.mcpServers === 'object') },\n  { label: 'patterson-assets has command + args array', test: (s, o) => { const sv = o && o.mcpServers && o.mcpServers['patterson-assets']; return !!(sv && typeof sv.command === 'string' && !sv.command.includes(' ') && Array.isArray(sv.args) && sv.args.length); } },\n  { label: 'Bundled paths use ${CLAUDE_PLUGIN_ROOT}', test: (s) => s.includes('${CLAUDE_PLUGIN_ROOT}') && !/\\/Users\\//.test(s) },",
  "jsonMode": true
 },
 {
  "id": "4-extending-claude-code/2-plugins/1-plugin-manifest",
  "num": 8,
  "part": "Extending Claude Code",
  "chapter": "Plugins",
  "title": "The plugin manifest",
  "artifact": "plugin.json",
  "html": "<h1>The plugin manifest</h1>\n<p>A <strong>plugin</strong> is a directory that bundles skills, commands, agents, hooks, and MCP servers into one installable unit. Its identity card is <code>.claude-plugin/plugin.json</code> — the <em>only</em> file that lives inside <code>.claude-plugin/</code>; every component directory (<code>skills/</code>, <code>agents/</code>, <code>hooks/</code>) sits at the plugin <strong>root</strong>.</p>\n<p>The rules that matter:</p>\n<ul><li><strong><code>name</code></strong> is required: kebab-case, no spaces. It becomes the namespace — the <code>new-deck</code> command in <code>patterson-deck</code> is invoked as <code>/patterson-deck:new-deck</code>.</li><li><strong><code>version</code></strong> is your update switch. Semver (<code>1.2.0</code>), and users only receive changes when you <strong>bump it</strong> — pushing commits alone does nothing.</li><li><strong><code>description</code></strong> is what users read in the <code>/plugin</code> picker. Non-empty, specific.</li></ul>\n<h2>Your task</h2>\n<p>The starter manifest fails <code>claude plugin validate</code> three ways. Fix it:</p>\n<div class=\"code-block\"><div class=\"code-title\">plugin.json</div><pre><code>{\n  \"name\": \"Patterson Deck Tools\",\n  \"name\": \"patterson-deck\",\n  \"version\": \"1.0.0\",\n  \"description\": \"Patterson 16:9 deck template with skills, a /new-deck command, and a review agent.\"\n}</code></pre></div>\n<div class=\"code-block\"><pre><code>node validate.mjs</code></pre></div>\n<div class=\"callout callout-info\"><div class=\"callout-title\">info</div><p>Real check, same idea: <code>claude plugin validate ./my-plugin --strict</code> in a terminal runs Anthropic's validator with warnings promoted to errors — wire it into CI.</p></div>",
  "starter": "{\n  \"name\": \"Patterson Deck Tools\",\n  \"description\": \"\"\n}\n",
  "solution": "{\n  \"name\": \"patterson-deck\",\n  \"version\": \"1.0.0\",\n  \"description\": \"Patterson 16:9 deck template with skills, a /new-deck command, and a review agent.\"\n}\n",
  "checksSrc": "  { label: 'Valid JSON', test: (s, o) => !!o },\n  { label: 'name is kebab-case (no spaces or uppercase)', test: (s, o) => !!(o && typeof o.name === 'string' && /^[a-z0-9]+(-[a-z0-9]+)*$/.test(o.name)) },\n  { label: 'version is semver — the update switch', test: (s, o) => !!(o && /^\\d+\\.\\d+\\.\\d+$/.test(o.version || '')) },\n  { label: 'description is non-empty and specific', test: (s, o) => !!(o && typeof o.description === 'string' && o.description.trim().length >= 20) },",
  "jsonMode": true
 },
 {
  "id": "4-extending-claude-code/3-marketplaces/1-marketplace-json",
  "num": 9,
  "part": "Extending Claude Code",
  "chapter": "Marketplaces",
  "title": "Publish a marketplace",
  "artifact": "marketplace.json",
  "html": "<h1>Publish a marketplace</h1>\n<p>A <strong>marketplace</strong> is just a git repo with a catalog file at <code>.claude-plugin/marketplace.json</code>. Users add it once —</p>\n<div class=\"code-block\"><pre><code>/plugin marketplace add patterson-agents/design-system</code></pre></div>\n<p>— and every plugin it lists becomes installable as <code>/plugin install &lt;plugin&gt;@patterson-design</code>.</p>\n<p>The catalog needs a <code>name</code>, an <code>owner</code> with at least a <code>name</code>, and a <code>plugins</code> array. Each entry needs its own <code>name</code> plus a <strong><code>source</code></strong> — for plugins that live in the same repo, a relative path starting with <code>./</code>.</p>\n<div class=\"callout callout-warn\"><div class=\"callout-title\">warn</div><p>The starter catalog is missing its <code>owner</code> and one plugin's <code>source</code> — the two errors that make <code>/plugin marketplace add</code> fail with the least helpful messages.</p></div>\n<h2>Your task</h2>\n<div class=\"code-block\"><div class=\"code-title\">marketplace.json</div><pre><code>{\n  \"name\": \"patterson-design\",\n  \"owner\": { \"name\": \"Patterson Companies Design System\" },\n  \"plugins\": [\n    { \"name\": \"patterson-brand\", \"source\": \"./plugins/patterson-brand\" },\n    { \"name\": \"patterson-deck\", \"source\": \"./plugins/patterson-deck\" }\n  ]\n}</code></pre></div>\n<div class=\"code-block\"><pre><code>node validate.mjs</code></pre></div>\n<div class=\"callout callout-success\"><div class=\"callout-title\">success</div><p>This is precisely the shape of the real Patterson marketplace you'll tour in the next part — nine plugins, one catalog, one <code>add</code> command.</p></div>",
  "starter": "{\n  \"name\": \"patterson-design\",\n  \"plugins\": [\n    { \"name\": \"patterson-brand\", \"source\": \"./plugins/patterson-brand\" },\n    { \"name\": \"patterson-deck\" }\n  ]\n}\n",
  "solution": "{\n  \"name\": \"patterson-design\",\n  \"owner\": { \"name\": \"Patterson Companies Design System\" },\n  \"plugins\": [\n    { \"name\": \"patterson-brand\", \"source\": \"./plugins/patterson-brand\" },\n    { \"name\": \"patterson-deck\", \"source\": \"./plugins/patterson-deck\" }\n  ]\n}\n",
  "checksSrc": "  { label: 'Valid JSON', test: (s, o) => !!o },\n  { label: 'Catalog has a name', test: (s, o) => !!(o && typeof o.name === 'string' && o.name.length) },\n  { label: 'owner.name identifies the maintainer', test: (s, o) => !!(o && o.owner && typeof o.owner.name === 'string' && o.owner.name.length) },\n  { label: 'Every plugin entry has name + relative ./ source', test: (s, o) => !!(o && Array.isArray(o.plugins) && o.plugins.length >= 2 && o.plugins.every(p => p && p.name && typeof p.source === 'string' && p.source.startsWith('./'))) },",
  "jsonMode": true
 },
 {
  "id": "5-patterson-marketplace/1-design-system-core/1-patterson-brand",
  "num": 10,
  "part": "The Patterson Marketplace",
  "chapter": "Design-system core",
  "title": "patterson-brand",
  "artifact": "setup.md",
  "html": "<h1>patterson-brand — the foundation</h1>\n<h2>What it ships</h2>\n<ul><li><strong>Skill</strong> <code>patterson-design</code> — the brand knowledge Claude loads when doing any Patterson work</li><li><strong>Commands</strong> <code>/patterson-brand:design</code> and <code>/patterson-brand:wire-tokens</code> (framework adapters: Tailwind v3/v4, UnoCSS, Theme UI, shadcn/ui)</li><li><strong>Agent</strong> a brand-guardian reviewer</li><li><strong><code>ds/</code> snapshot</strong> — <code>tokens/colors.css</code>, fonts, logos, brand imagery, the React component library</li></ul>\n<h2>When to use it</h2>\n<p>Install it first, always — every other Patterson plugin assumes its tokens and fonts. It is the plugin form of the <code>design.md</code> you wrote in Part 2.</p>\n<div class=\"callout callout-info\"><div class=\"callout-title\">info</div><p><strong>What you'll build:</strong> a <code>setup.md</code> runbook a teammate can follow verbatim to install and try <strong>patterson-brand</strong>. <strong>How it's checked:</strong> <code>node validate.mjs</code> lints the runbook for the exact commands and the one fact people always forget.</p></div>\n<h2>Your task</h2>\n<p>Complete <code>setup.md</code>:</p>\n<p><strong>1.</strong> In the <em>Install</em> block, add the marketplace and install the plugin:</p>\n<div class=\"code-block\"><pre><code>/plugin marketplace add patterson-agents/design-system\n/plugin install patterson-brand@patterson-design</code></pre></div>\n<p><strong>2.</strong> Under <em>Try it</em>, invoke the plugin's command — <code>/patterson-brand:design</code> (loads the full design context (tokens, voice, component rules)).</p>\n<p><strong>3.</strong> Under <em>Remember</em>, record the key fact: <em>Tokens land in ds/tokens/colors.css — reference them, never raw hex.</em></p>\n<div class=\"code-block\"><pre><code>node validate.mjs</code></pre></div>\n<div class=\"callout callout-tip\"><div class=\"callout-title\">tip</div><p>Plugin commands are always namespaced <code>/&lt;plugin&gt;:&lt;command&gt;</code> — the plugin's <code>name</code> in <code>plugin.json</code> is the namespace. That's why the catalog insisted on kebab-case.</p></div>",
  "starter": "# Setup — patterson-brand\n\n## Install\n\n```text\n(add the marketplace, then install patterson-brand)\n```\n\n## Try it\n\n(invoke the plugin's slash command)\n\n## Remember\n\n(the one fact people forget about this plugin)\n",
  "solution": "# Setup — patterson-brand\n\n## Install\n\n```text\n/plugin marketplace add patterson-agents/design-system\n/plugin install patterson-brand@patterson-design\n```\n\n## Try it\n\n/patterson-brand:design\n\n## Remember\n\nTokens land in ds/tokens/colors.css — reference them, never raw hex.\n",
  "checksSrc": "  { label: 'Adds the marketplace from patterson-agents/design-system', test: (s) => /\\/plugin marketplace add patterson-agents\\/design-system/.test(s) },\n  { label: 'Installs patterson-brand from the patterson-design marketplace', test: (s) => /\\/plugin install patterson-brand@patterson-design/.test(s) },\n  { label: 'Tries the namespaced command /patterson-brand:design', test: (s) => /\\/patterson-brand:design/.test(s) },\n  { label: 'Mentions the token file the snapshot ships (tokens/colors.css)', test: (s) => new RegExp('tokens\\/colors\\.css', 'i').test(s) },",
  "jsonMode": false
 },
 {
  "id": "5-patterson-marketplace/2-templates/1-patterson-deck",
  "num": 11,
  "part": "The Patterson Marketplace",
  "chapter": "Templates",
  "title": "patterson-deck",
  "artifact": "setup.md",
  "html": "<h1>patterson-deck — 16:9 presentations</h1>\n<h2>What it ships</h2>\n<ul><li><strong>Skill</strong> <code>deck-template</code> — slide recipes: cover, section divider, stats, comparison, quote, capabilities table, full-bleed photo band, closing</li><li><strong>Command</strong> <code>/patterson-deck:new-deck</code></li><li><strong>Agent</strong> a deck reviewer</li><li><strong><code>ds/</code> snapshot</strong> — the static-HTML 16:9 deck template with wave watermark and brand imagery</li></ul>\n<h2>When to use it</h2>\n<p>Reach for it whenever the ask is \"make a deck / slides / presentation\" — output is print-to-PDF-ready static HTML at 1920×1080.</p>\n<div class=\"callout callout-info\"><div class=\"callout-title\">info</div><p><strong>What you'll build:</strong> a <code>setup.md</code> runbook a teammate can follow verbatim to install and try <strong>patterson-deck</strong>. <strong>How it's checked:</strong> <code>node validate.mjs</code> lints the runbook for the exact commands and the one fact people always forget.</p></div>\n<h2>Your task</h2>\n<p>Complete <code>setup.md</code>:</p>\n<p><strong>1.</strong> In the <em>Install</em> block, add the marketplace and install the plugin:</p>\n<div class=\"code-block\"><pre><code>/plugin marketplace add patterson-agents/design-system\n/plugin install patterson-deck@patterson-design</code></pre></div>\n<p><strong>2.</strong> Under <em>Try it</em>, invoke the plugin's command — <code>/patterson-deck:new-deck</code> (scaffolds a deck from the template).</p>\n<p><strong>3.</strong> Under <em>Remember</em>, record the key fact: <em>Slides are fixed 16:9 (1920×1080) — print-to-PDF ready.</em></p>\n<div class=\"code-block\"><pre><code>node validate.mjs</code></pre></div>\n<div class=\"callout callout-tip\"><div class=\"callout-title\">tip</div><p>Plugin commands are always namespaced <code>/&lt;plugin&gt;:&lt;command&gt;</code> — the plugin's <code>name</code> in <code>plugin.json</code> is the namespace. That's why the catalog insisted on kebab-case.</p></div>",
  "starter": "# Setup — patterson-deck\n\n## Install\n\n```text\n(add the marketplace, then install patterson-deck)\n```\n\n## Try it\n\n(invoke the plugin's slash command)\n\n## Remember\n\n(the one fact people forget about this plugin)\n",
  "solution": "# Setup — patterson-deck\n\n## Install\n\n```text\n/plugin marketplace add patterson-agents/design-system\n/plugin install patterson-deck@patterson-design\n```\n\n## Try it\n\n/patterson-deck:new-deck\n\n## Remember\n\nSlides are fixed 16:9 (1920×1080) — print-to-PDF ready.\n",
  "checksSrc": "  { label: 'Adds the marketplace from patterson-agents/design-system', test: (s) => /\\/plugin marketplace add patterson-agents\\/design-system/.test(s) },\n  { label: 'Installs patterson-deck from the patterson-design marketplace', test: (s) => /\\/plugin install patterson-deck@patterson-design/.test(s) },\n  { label: 'Tries the namespaced command /patterson-deck:new-deck', test: (s) => /\\/patterson-deck:new-deck/.test(s) },\n  { label: 'Notes the 16:9 / 1920×1080 canvas', test: (s) => new RegExp('(16:9|1920)', 'i').test(s) },",
  "jsonMode": false
 },
 {
  "id": "5-patterson-marketplace/2-templates/2-patterson-executive-deck",
  "num": 12,
  "part": "The Patterson Marketplace",
  "chapter": "Templates",
  "title": "patterson-executive-deck",
  "artifact": "setup.md",
  "html": "<h1>patterson-executive-deck — briefings</h1>\n<h2>What it ships</h2>\n<ul><li><strong>Skill</strong> <code>executive-deck-template</code> — editorial navy cover, key takeaways, breakdown matrix, requirements, build outputs, benefits slides</li><li><strong>Command</strong> <code>/patterson-executive-deck:new-executive-deck</code></li><li><strong>Agent</strong> an executive-tone reviewer</li><li><strong><code>ds/</code> snapshot</strong> — the executive deck template</li></ul>\n<h2>When to use it</h2>\n<p>Denser and more formal than <code>patterson-deck</code> — use for leadership briefings, program reviews, and anything with a decision at the end.</p>\n<div class=\"callout callout-info\"><div class=\"callout-title\">info</div><p><strong>What you'll build:</strong> a <code>setup.md</code> runbook a teammate can follow verbatim to install and try <strong>patterson-executive-deck</strong>. <strong>How it's checked:</strong> <code>node validate.mjs</code> lints the runbook for the exact commands and the one fact people always forget.</p></div>\n<h2>Your task</h2>\n<p>Complete <code>setup.md</code>:</p>\n<p><strong>1.</strong> In the <em>Install</em> block, add the marketplace and install the plugin:</p>\n<div class=\"code-block\"><pre><code>/plugin marketplace add patterson-agents/design-system\n/plugin install patterson-executive-deck@patterson-design</code></pre></div>\n<p><strong>2.</strong> Under <em>Try it</em>, invoke the plugin's command — <code>/patterson-executive-deck:new-executive-deck</code> (scaffolds the denser executive variant).</p>\n<p><strong>3.</strong> Under <em>Remember</em>, record the key fact: <em>Denser and more formal than the standard deck — built for leadership briefings.</em></p>\n<div class=\"code-block\"><pre><code>node validate.mjs</code></pre></div>\n<div class=\"callout callout-tip\"><div class=\"callout-title\">tip</div><p>Plugin commands are always namespaced <code>/&lt;plugin&gt;:&lt;command&gt;</code> — the plugin's <code>name</code> in <code>plugin.json</code> is the namespace. That's why the catalog insisted on kebab-case.</p></div>",
  "starter": "# Setup — patterson-executive-deck\n\n## Install\n\n```text\n(add the marketplace, then install patterson-executive-deck)\n```\n\n## Try it\n\n(invoke the plugin's slash command)\n\n## Remember\n\n(the one fact people forget about this plugin)\n",
  "solution": "# Setup — patterson-executive-deck\n\n## Install\n\n```text\n/plugin marketplace add patterson-agents/design-system\n/plugin install patterson-executive-deck@patterson-design\n```\n\n## Try it\n\n/patterson-executive-deck:new-executive-deck\n\n## Remember\n\nDenser and more formal than the standard deck — built for leadership briefings.\n",
  "checksSrc": "  { label: 'Adds the marketplace from patterson-agents/design-system', test: (s) => /\\/plugin marketplace add patterson-agents\\/design-system/.test(s) },\n  { label: 'Installs patterson-executive-deck from the patterson-design marketplace', test: (s) => /\\/plugin install patterson-executive-deck@patterson-design/.test(s) },\n  { label: 'Tries the namespaced command /patterson-executive-deck:new-executive-deck', test: (s) => /\\/patterson-executive-deck:new-executive-deck/.test(s) },\n  { label: 'Distinguishes it from the standard deck (denser / formal / executive)', test: (s) => new RegExp('(denser|formal|executive briefing|leadership)', 'i').test(s) },",
  "jsonMode": false
 },
 {
  "id": "5-patterson-marketplace/2-templates/3-patterson-corporate-page",
  "num": 13,
  "part": "The Patterson Marketplace",
  "chapter": "Templates",
  "title": "patterson-corporate-page",
  "artifact": "setup.md",
  "html": "<h1>patterson-corporate-page — web page shell</h1>\n<h2>What it ships</h2>\n<ul><li><strong>Skill</strong> <code>corporate-page-template</code> — sticky nav with logo, navy hero, content band, footer</li><li><strong>Command</strong> <code>/patterson-corporate-page:new-page</code></li><li><strong>Agent</strong> a page reviewer</li><li><strong><code>ds/</code> snapshot</strong> — the React page shell</li></ul>\n<h2>When to use it</h2>\n<p>The starting point for any on-brand marketing or informational page — hero copy in, page out.</p>\n<div class=\"callout callout-info\"><div class=\"callout-title\">info</div><p><strong>What you'll build:</strong> a <code>setup.md</code> runbook a teammate can follow verbatim to install and try <strong>patterson-corporate-page</strong>. <strong>How it's checked:</strong> <code>node validate.mjs</code> lints the runbook for the exact commands and the one fact people always forget.</p></div>\n<h2>Your task</h2>\n<p>Complete <code>setup.md</code>:</p>\n<p><strong>1.</strong> In the <em>Install</em> block, add the marketplace and install the plugin:</p>\n<div class=\"code-block\"><pre><code>/plugin marketplace add patterson-agents/design-system\n/plugin install patterson-corporate-page@patterson-design</code></pre></div>\n<p><strong>2.</strong> Under <em>Try it</em>, invoke the plugin's command — <code>/patterson-corporate-page:new-page</code> (scaffolds the page shell).</p>\n<p><strong>3.</strong> Under <em>Remember</em>, record the key fact: <em>Shell parts: sticky nav with logo → navy hero → content band → footer.</em></p>\n<div class=\"code-block\"><pre><code>node validate.mjs</code></pre></div>\n<div class=\"callout callout-tip\"><div class=\"callout-title\">tip</div><p>Plugin commands are always namespaced <code>/&lt;plugin&gt;:&lt;command&gt;</code> — the plugin's <code>name</code> in <code>plugin.json</code> is the namespace. That's why the catalog insisted on kebab-case.</p></div>",
  "starter": "# Setup — patterson-corporate-page\n\n## Install\n\n```text\n(add the marketplace, then install patterson-corporate-page)\n```\n\n## Try it\n\n(invoke the plugin's slash command)\n\n## Remember\n\n(the one fact people forget about this plugin)\n",
  "solution": "# Setup — patterson-corporate-page\n\n## Install\n\n```text\n/plugin marketplace add patterson-agents/design-system\n/plugin install patterson-corporate-page@patterson-design\n```\n\n## Try it\n\n/patterson-corporate-page:new-page\n\n## Remember\n\nShell parts: sticky nav with logo → navy hero → content band → footer.\n",
  "checksSrc": "  { label: 'Adds the marketplace from patterson-agents/design-system', test: (s) => /\\/plugin marketplace add patterson-agents\\/design-system/.test(s) },\n  { label: 'Installs patterson-corporate-page from the patterson-design marketplace', test: (s) => /\\/plugin install patterson-corporate-page@patterson-design/.test(s) },\n  { label: 'Tries the namespaced command /patterson-corporate-page:new-page', test: (s) => /\\/patterson-corporate-page:new-page/.test(s) },\n  { label: 'Names the shell parts (nav, hero, footer)', test: (s) => new RegExp('(sticky nav|navy hero|footer)', 'i').test(s) },",
  "jsonMode": false
 },
 {
  "id": "5-patterson-marketplace/2-templates/4-patterson-file-manager",
  "num": 14,
  "part": "The Patterson Marketplace",
  "chapter": "Templates",
  "title": "patterson-file-manager",
  "artifact": "setup.md",
  "html": "<h1>patterson-file-manager — app shell</h1>\n<h2>What it ships</h2>\n<ul><li><strong>Skill</strong> <code>file-manager-template</code> — navy top bar, sidebar tree, content grid (\"Skill Studio\")</li><li><strong>Command</strong> <code>/patterson-file-manager:new-app-shell</code></li><li><strong>Agent</strong> an app-shell reviewer</li><li><strong><code>ds/</code> snapshot</strong> — the React prototype shell</li></ul>\n<h2>When to use it</h2>\n<p>Use for internal tools and dashboard-style prototypes — anything with a tree on the left and content on the right.</p>\n<div class=\"callout callout-info\"><div class=\"callout-title\">info</div><p><strong>What you'll build:</strong> a <code>setup.md</code> runbook a teammate can follow verbatim to install and try <strong>patterson-file-manager</strong>. <strong>How it's checked:</strong> <code>node validate.mjs</code> lints the runbook for the exact commands and the one fact people always forget.</p></div>\n<h2>Your task</h2>\n<p>Complete <code>setup.md</code>:</p>\n<p><strong>1.</strong> In the <em>Install</em> block, add the marketplace and install the plugin:</p>\n<div class=\"code-block\"><pre><code>/plugin marketplace add patterson-agents/design-system\n/plugin install patterson-file-manager@patterson-design</code></pre></div>\n<p><strong>2.</strong> Under <em>Try it</em>, invoke the plugin's command — <code>/patterson-file-manager:new-app-shell</code> (scaffolds the Skill Studio shell).</p>\n<p><strong>3.</strong> Under <em>Remember</em>, record the key fact: <em>Layout: navy top bar, sidebar tree, content grid.</em></p>\n<div class=\"code-block\"><pre><code>node validate.mjs</code></pre></div>\n<div class=\"callout callout-tip\"><div class=\"callout-title\">tip</div><p>Plugin commands are always namespaced <code>/&lt;plugin&gt;:&lt;command&gt;</code> — the plugin's <code>name</code> in <code>plugin.json</code> is the namespace. That's why the catalog insisted on kebab-case.</p></div>",
  "starter": "# Setup — patterson-file-manager\n\n## Install\n\n```text\n(add the marketplace, then install patterson-file-manager)\n```\n\n## Try it\n\n(invoke the plugin's slash command)\n\n## Remember\n\n(the one fact people forget about this plugin)\n",
  "solution": "# Setup — patterson-file-manager\n\n## Install\n\n```text\n/plugin marketplace add patterson-agents/design-system\n/plugin install patterson-file-manager@patterson-design\n```\n\n## Try it\n\n/patterson-file-manager:new-app-shell\n\n## Remember\n\nLayout: navy top bar, sidebar tree, content grid.\n",
  "checksSrc": "  { label: 'Adds the marketplace from patterson-agents/design-system', test: (s) => /\\/plugin marketplace add patterson-agents\\/design-system/.test(s) },\n  { label: 'Installs patterson-file-manager from the patterson-design marketplace', test: (s) => /\\/plugin install patterson-file-manager@patterson-design/.test(s) },\n  { label: 'Tries the namespaced command /patterson-file-manager:new-app-shell', test: (s) => /\\/patterson-file-manager:new-app-shell/.test(s) },\n  { label: 'Names the shell layout (top bar, sidebar tree, content grid)', test: (s) => new RegExp('(sidebar tree|content grid|top bar)', 'i').test(s) },",
  "jsonMode": false
 },
 {
  "id": "5-patterson-marketplace/2-templates/5-patterson-docs",
  "num": 15,
  "part": "The Patterson Marketplace",
  "chapter": "Templates",
  "title": "patterson-docs",
  "artifact": "setup.md",
  "html": "<h1>patterson-docs — documentation sites</h1>\n<h2>What it ships</h2>\n<ul><li><strong>Skill</strong> <code>docs-site</code> — VitePress-styled, Diátaxis-organized docs UI (sidebar nav, content pages, collections)</li><li><strong>Command</strong> <code>/patterson-docs:new-docs</code></li><li><strong>Agent</strong> a docs reviewer</li><li><strong><code>ds/</code> snapshot</strong> — the docs UI kit + a browser-openable page template</li></ul>\n<h2>When to use it</h2>\n<p>Use for any documentation site or knowledge base. Content is organized by Diátaxis: tutorials, how-to guides, reference, explanation.</p>\n<div class=\"callout callout-info\"><div class=\"callout-title\">info</div><p><strong>What you'll build:</strong> a <code>setup.md</code> runbook a teammate can follow verbatim to install and try <strong>patterson-docs</strong>. <strong>How it's checked:</strong> <code>node validate.mjs</code> lints the runbook for the exact commands and the one fact people always forget.</p></div>\n<h2>Your task</h2>\n<p>Complete <code>setup.md</code>:</p>\n<p><strong>1.</strong> In the <em>Install</em> block, add the marketplace and install the plugin:</p>\n<div class=\"code-block\"><pre><code>/plugin marketplace add patterson-agents/design-system\n/plugin install patterson-docs@patterson-design</code></pre></div>\n<p><strong>2.</strong> Under <em>Try it</em>, invoke the plugin's command — <code>/patterson-docs:new-docs</code> (scaffolds the docs page).</p>\n<p><strong>3.</strong> Under <em>Remember</em>, record the key fact: <em>Docs follow Diátaxis: tutorials / how-to / reference / explanation.</em></p>\n<div class=\"code-block\"><pre><code>node validate.mjs</code></pre></div>\n<div class=\"callout callout-tip\"><div class=\"callout-title\">tip</div><p>Plugin commands are always namespaced <code>/&lt;plugin&gt;:&lt;command&gt;</code> — the plugin's <code>name</code> in <code>plugin.json</code> is the namespace. That's why the catalog insisted on kebab-case.</p></div>",
  "starter": "# Setup — patterson-docs\n\n## Install\n\n```text\n(add the marketplace, then install patterson-docs)\n```\n\n## Try it\n\n(invoke the plugin's slash command)\n\n## Remember\n\n(the one fact people forget about this plugin)\n",
  "solution": "# Setup — patterson-docs\n\n## Install\n\n```text\n/plugin marketplace add patterson-agents/design-system\n/plugin install patterson-docs@patterson-design\n```\n\n## Try it\n\n/patterson-docs:new-docs\n\n## Remember\n\nDocs follow Diátaxis: tutorials / how-to / reference / explanation.\n",
  "checksSrc": "  { label: 'Adds the marketplace from patterson-agents/design-system', test: (s) => /\\/plugin marketplace add patterson-agents\\/design-system/.test(s) },\n  { label: 'Installs patterson-docs from the patterson-design marketplace', test: (s) => /\\/plugin install patterson-docs@patterson-design/.test(s) },\n  { label: 'Tries the namespaced command /patterson-docs:new-docs', test: (s) => /\\/patterson-docs:new-docs/.test(s) },\n  { label: 'Mentions the Diátaxis organization', test: (s) => new RegExp('Di(á|a)taxis', 'i').test(s) },",
  "jsonMode": false
 },
 {
  "id": "5-patterson-marketplace/2-templates/6-patterson-tutorialkit",
  "num": 16,
  "part": "The Patterson Marketplace",
  "chapter": "Templates",
  "title": "patterson-tutorialkit",
  "artifact": "setup.md",
  "html": "<h1>patterson-tutorialkit — this very tutorial</h1>\n<h2>What it ships</h2>\n<ul><li><strong>Skill</strong> <code>tutorialkit-theme</code> — the canonical <code>theme.css</code> (--tk-* surface mapped to Patterson tokens, 4 themes)</li><li><strong>Command</strong> <code>/patterson-tutorialkit:brand-tutorialkit</code></li><li><strong>Agent</strong> a theme reviewer</li><li><strong><code>ds/</code> snapshot</strong> — the runnable Astro starter you are reading right now</li></ul>\n<h2>When to use it</h2>\n<p>Use to spin up interactive, in-browser workshops (like this one). Lessons run in a WebContainer, so the hands-on loop is always author → <code>node validate.mjs</code> → pass/fail.</p>\n<div class=\"callout callout-info\"><div class=\"callout-title\">info</div><p><strong>What you'll build:</strong> a <code>setup.md</code> runbook a teammate can follow verbatim to install and try <strong>patterson-tutorialkit</strong>. <strong>How it's checked:</strong> <code>node validate.mjs</code> lints the runbook for the exact commands and the one fact people always forget.</p></div>\n<h2>Your task</h2>\n<p>Complete <code>setup.md</code>:</p>\n<p><strong>1.</strong> In the <em>Install</em> block, add the marketplace and install the plugin:</p>\n<div class=\"code-block\"><pre><code>/plugin marketplace add patterson-agents/design-system\n/plugin install patterson-tutorialkit@patterson-design</code></pre></div>\n<p><strong>2.</strong> Under <em>Try it</em>, invoke the plugin's command — <code>/patterson-tutorialkit:brand-tutorialkit</code> (applies the Patterson theme to a TutorialKit project).</p>\n<p><strong>3.</strong> Under <em>Remember</em>, record the key fact: <em>The whole brand lives in theme.css — every --tk-</em> var traces to a Patterson token.*</p>\n<div class=\"code-block\"><pre><code>node validate.mjs</code></pre></div>\n<div class=\"callout callout-tip\"><div class=\"callout-title\">tip</div><p>Plugin commands are always namespaced <code>/&lt;plugin&gt;:&lt;command&gt;</code> — the plugin's <code>name</code> in <code>plugin.json</code> is the namespace. That's why the catalog insisted on kebab-case.</p></div>",
  "starter": "# Setup — patterson-tutorialkit\n\n## Install\n\n```text\n(add the marketplace, then install patterson-tutorialkit)\n```\n\n## Try it\n\n(invoke the plugin's slash command)\n\n## Remember\n\n(the one fact people forget about this plugin)\n",
  "solution": "# Setup — patterson-tutorialkit\n\n## Install\n\n```text\n/plugin marketplace add patterson-agents/design-system\n/plugin install patterson-tutorialkit@patterson-design\n```\n\n## Try it\n\n/patterson-tutorialkit:brand-tutorialkit\n\n## Remember\n\nThe whole brand lives in theme.css — every --tk-* var traces to a Patterson token.\n",
  "checksSrc": "  { label: 'Adds the marketplace from patterson-agents/design-system', test: (s) => /\\/plugin marketplace add patterson-agents\\/design-system/.test(s) },\n  { label: 'Installs patterson-tutorialkit from the patterson-design marketplace', test: (s) => /\\/plugin install patterson-tutorialkit@patterson-design/.test(s) },\n  { label: 'Tries the namespaced command /patterson-tutorialkit:brand-tutorialkit', test: (s) => /\\/patterson-tutorialkit:brand-tutorialkit/.test(s) },\n  { label: 'Names the theme file (theme.css)', test: (s) => new RegExp('theme\\.css', 'i').test(s) },",
  "jsonMode": false
 },
 {
  "id": "5-patterson-marketplace/3-ui-kits/1-patterson-corporate-website",
  "num": 17,
  "part": "The Patterson Marketplace",
  "chapter": "UI kits",
  "title": "patterson-corporate-website",
  "artifact": "setup.md",
  "html": "<h1>patterson-corporate-website — the .com kit</h1>\n<h2>What it ships</h2>\n<ul><li><strong>Skill</strong> <code>corporate-website-kit</code> — home hero, We-Are-Patterson stats, capability tabs, What-We-Do, newsroom, header, footer as separate React screens</li><li><strong>Command</strong> <code>/patterson-corporate-website:new-corporate-site</code></li><li><strong>Agent</strong> a site reviewer</li><li><strong><code>ds/</code> snapshot</strong> — the interactive recreation of pattersoncompanies.com</li></ul>\n<h2>When to use it</h2>\n<p>Use when recreating or extending the corporate site — every section ships as its own screen you can compose.</p>\n<div class=\"callout callout-info\"><div class=\"callout-title\">info</div><p><strong>What you'll build:</strong> a <code>setup.md</code> runbook a teammate can follow verbatim to install and try <strong>patterson-corporate-website</strong>. <strong>How it's checked:</strong> <code>node validate.mjs</code> lints the runbook for the exact commands and the one fact people always forget.</p></div>\n<h2>Your task</h2>\n<p>Complete <code>setup.md</code>:</p>\n<p><strong>1.</strong> In the <em>Install</em> block, add the marketplace and install the plugin:</p>\n<div class=\"code-block\"><pre><code>/plugin marketplace add patterson-agents/design-system\n/plugin install patterson-corporate-website@patterson-design</code></pre></div>\n<p><strong>2.</strong> Under <em>Try it</em>, invoke the plugin's command — <code>/patterson-corporate-website:new-corporate-site</code> (assembles screens from the kit).</p>\n<p><strong>3.</strong> Under <em>Remember</em>, record the key fact: <em>Screens include: home hero, stats band, capability tabs, newsroom, header, footer.</em></p>\n<div class=\"code-block\"><pre><code>node validate.mjs</code></pre></div>\n<div class=\"callout callout-tip\"><div class=\"callout-title\">tip</div><p>Plugin commands are always namespaced <code>/&lt;plugin&gt;:&lt;command&gt;</code> — the plugin's <code>name</code> in <code>plugin.json</code> is the namespace. That's why the catalog insisted on kebab-case.</p></div>",
  "starter": "# Setup — patterson-corporate-website\n\n## Install\n\n```text\n(add the marketplace, then install patterson-corporate-website)\n```\n\n## Try it\n\n(invoke the plugin's slash command)\n\n## Remember\n\n(the one fact people forget about this plugin)\n",
  "solution": "# Setup — patterson-corporate-website\n\n## Install\n\n```text\n/plugin marketplace add patterson-agents/design-system\n/plugin install patterson-corporate-website@patterson-design\n```\n\n## Try it\n\n/patterson-corporate-website:new-corporate-site\n\n## Remember\n\nScreens include: home hero, stats band, capability tabs, newsroom, header, footer.\n",
  "checksSrc": "  { label: 'Adds the marketplace from patterson-agents/design-system', test: (s) => /\\/plugin marketplace add patterson-agents\\/design-system/.test(s) },\n  { label: 'Installs patterson-corporate-website from the patterson-design marketplace', test: (s) => /\\/plugin install patterson-corporate-website@patterson-design/.test(s) },\n  { label: 'Tries the namespaced command /patterson-corporate-website:new-corporate-site', test: (s) => /\\/patterson-corporate-website:new-corporate-site/.test(s) },\n  { label: 'Names at least one kit screen (hero / stats / newsroom)', test: (s) => new RegExp('(hero|stats|newsroom|capability tabs)', 'i').test(s) },",
  "jsonMode": false
 },
 {
  "id": "5-patterson-marketplace/3-ui-kits/2-patterson-storefront",
  "num": 18,
  "part": "The Patterson Marketplace",
  "chapter": "UI kits",
  "title": "patterson-storefront",
  "artifact": "setup.md",
  "html": "<h1>patterson-storefront — e-commerce kit</h1>\n<h2>What it ships</h2>\n<ul><li><strong>Skill</strong> <code>storefront-kit</code> — utility bar, search, category nav + flyouts, hero, featured products, manufacturers, rewards, footer</li><li><strong>Command</strong> <code>/patterson-storefront:new-storefront</code></li><li><strong>Agent</strong> a storefront reviewer</li><li><strong><code>ds/</code> snapshot</strong> — the pattern-library v5.7.2 shell, switchable Dental ↔ Veterinary</li></ul>\n<h2>When to use it</h2>\n<p>Use for anything shop-shaped. One switch swaps the whole brand between Dental and Veterinary.</p>\n<div class=\"callout callout-info\"><div class=\"callout-title\">info</div><p><strong>What you'll build:</strong> a <code>setup.md</code> runbook a teammate can follow verbatim to install and try <strong>patterson-storefront</strong>. <strong>How it's checked:</strong> <code>node validate.mjs</code> lints the runbook for the exact commands and the one fact people always forget.</p></div>\n<h2>Your task</h2>\n<p>Complete <code>setup.md</code>:</p>\n<p><strong>1.</strong> In the <em>Install</em> block, add the marketplace and install the plugin:</p>\n<div class=\"code-block\"><pre><code>/plugin marketplace add patterson-agents/design-system\n/plugin install patterson-storefront@patterson-design</code></pre></div>\n<p><strong>2.</strong> Under <em>Try it</em>, invoke the plugin's command — <code>/patterson-storefront:new-storefront</code> (assembles the storefront shell).</p>\n<p><strong>3.</strong> Under <em>Remember</em>, record the key fact: <em>One switch swaps the whole storefront between Dental and Veterinary brands.</em></p>\n<div class=\"code-block\"><pre><code>node validate.mjs</code></pre></div>\n<div class=\"callout callout-tip\"><div class=\"callout-title\">tip</div><p>Plugin commands are always namespaced <code>/&lt;plugin&gt;:&lt;command&gt;</code> — the plugin's <code>name</code> in <code>plugin.json</code> is the namespace. That's why the catalog insisted on kebab-case.</p></div>",
  "starter": "# Setup — patterson-storefront\n\n## Install\n\n```text\n(add the marketplace, then install patterson-storefront)\n```\n\n## Try it\n\n(invoke the plugin's slash command)\n\n## Remember\n\n(the one fact people forget about this plugin)\n",
  "solution": "# Setup — patterson-storefront\n\n## Install\n\n```text\n/plugin marketplace add patterson-agents/design-system\n/plugin install patterson-storefront@patterson-design\n```\n\n## Try it\n\n/patterson-storefront:new-storefront\n\n## Remember\n\nOne switch swaps the whole storefront between Dental and Veterinary brands.\n",
  "checksSrc": "  { label: 'Adds the marketplace from patterson-agents/design-system', test: (s) => /\\/plugin marketplace add patterson-agents\\/design-system/.test(s) },\n  { label: 'Installs patterson-storefront from the patterson-design marketplace', test: (s) => /\\/plugin install patterson-storefront@patterson-design/.test(s) },\n  { label: 'Tries the namespaced command /patterson-storefront:new-storefront', test: (s) => /\\/patterson-storefront:new-storefront/.test(s) },\n  { label: 'Mentions the Dental ↔ Veterinary brand switch', test: (s) => new RegExp('(Dental|Veterinary)', 'i').test(s) },",
  "jsonMode": false
 }
];
